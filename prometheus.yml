global:
  scrape_interval: 60s
  evaluation_interval: 60s

scrape_configs:
  - job_name: 'ecoflow'
    static_configs:
      - targets: ['localhost:9090']
        labels:
          instance: 'ecoflowmon'
